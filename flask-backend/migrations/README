
# Run the following commands to make another migration:

### Single-database configuration for Flask.

#### Generate a New Migration:
```bash
flask db migrate -m "Commit message"
```

#### Apply the Migration:
```bash
flask db upgrade
```
